<!-- FILE: detallePedido.ejs -->
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del Pedido</title>
    <link rel="stylesheet" href="/css/navbar.css">
    <style>
        /* Estilos generales */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        /* Contenedor principal */
        .detail-container {
            max-width: 800px;
            margin: 2rem auto;
            background: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .detail-container h2 {
            text-align: center;
            color: #333;
            margin-bottom: 1.5rem;
        }

        /* Información del pedido */
        .order-info {
            margin-bottom: 2rem;
        }

        .order-info p {
            margin: 0.5rem 0;
            font-size: 1rem;
            color: #333;
        }

        .order-info .status {
            font-weight: bold;
            color: #007bff;
        }

        /* Lista de productos */
        .products-list {
            width: 100%;
            border-collapse: collapse;
        }

        .products-list th, .products-list td {
            border: 1px solid #ddd;
            padding: 0.8rem;
            text-align: left;
        }

        .products-list th {
            background-color: #f2f2f2;
        }

        .product-image {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 8px;
        }

        /* Total */
        .total {
            text-align: right;
            margin-top: 1rem;
            font-size: 1.2rem;
            font-weight: bold;
        }

        /* Botón volver */
        .back-button {
            background-color: #ffcc00;
            color: #333;
            border: none;
            padding: 0.8rem 1.2rem;
            font-size: 1rem;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            margin-top: 1rem;
        }

        .back-button:hover {
            background-color: #e6b800;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <ul class="navbar-menu">
            <li><a href="/user/home">Home</a></li>
            <li><a href="/user/perfil">Mi perfil</a></li>
            <li><a href="/user/pedidos">Mis pedidos</a></li>
            <li><a href="/user/direcciones">Mis direcciones</a></li>
            <li><a href="/user/favoritos">Mis favoritos</a></li>
            <li><a href="/user/logout">Cerrar sesión</a></li>
        </ul>
    </nav>
    
    <div class="detail-container">
        <h2>Detalle del Pedido</h2>
        <div class="order-info">
            <p class="status">Estado: <%= order.status %></p>
            <p>Comercio: <%= order.business.business_name %></p>
            <p>Fecha y hora: <%= new Date(order.createdAt).toLocaleString() %></p>
        </div>
        <table class="products-list">
            <thead>
                <tr>
                    <th>Imagen</th>
                    <th>Producto</th>
                    <th>Precio</th>
                </tr>
            </thead>
            <tbody>
                <% order.products.forEach(product => { %>
                    <tr>
                        <td><img src="<%= product.image %>" alt="<%= product.name %>" class="product-image"></td>
                        <td><%= product.name %></td>
                        <td>$<%= product.price.toFixed(2) %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        <div class="total">Total: $<%= order.total.toFixed(2) %></div>
        <button class="back-button" onclick="window.location.href='/user/pedidos'">Volver a Mis Pedidos</button>
    </div>
</body>
</html>